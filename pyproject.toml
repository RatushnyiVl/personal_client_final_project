[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "personal_client_final_project"
version = "1.0.0"
requires-python = ">=3.10"

[project.scripts]
assistant = "personal_assistant.main:main"